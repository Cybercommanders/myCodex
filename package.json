{
  "name": "@cybercommanders/mycodex",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "myCodex — use Codex from Claude Code to review code or delegate tasks.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "bump-version": "node scripts/bump-version.mjs",
    "check-version": "node scripts/bump-version.mjs --check",
    "prebuild": "mkdir -p plugins/codex/.generated/app-server-types && codex app-server generate-ts --out plugins/codex/.generated/app-server-types",
    "build": "tsc -p tsconfig.app-server.json",
    "test": "node --test tests/*.test.mjs",
    "lint": "eslint . --ext .mjs,.js"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "eslint": "^10.5.0",
    "globals": "^17.7.0",
    "typescript": "^6.0.2"
  }
}
